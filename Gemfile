source "https://rubygems.org"

# Use the same gem set GitHub Pages runs, so local previews match production.
gem "github-pages", group: :jekyll_plugins

# Needed to run `jekyll serve` on Ruby 3+
gem "webrick"
